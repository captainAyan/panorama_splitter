export const AspectRatio = Object.freeze({
  FourToFive: 4 / 5,
  Square: 1,
});

export const FillColor = Object.freeze({
  WHITE: "white",
  BLACK: "black",
});

export const DefaultPadding = 10;

export const CroppingSettings = Object.freeze({
  ALLOW_CROPPING: "allow_cropping",
  NO_CROPPING: "no_cropping",
});
